source "https://rubygems.org"

# GitHub Pages pins versions compatible with their build environment.
# If you build locally, you can `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins
